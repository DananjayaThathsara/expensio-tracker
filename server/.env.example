# --- Database (Neon) ---
# Neon dashboard -> your project -> Connection string (pooled). Keep ?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_zxkZGulgLT06@ep-lively-feather-azjhng8e-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# --- App ---
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173
SERVER_ORIGIN=http://localhost:4000
NODE_ENV=development

# --- Google OAuth (console.cloud.google.com -> APIs & Services -> Credentials) ---
# Authorised redirect URI: {SERVER_ORIGIN}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- GitHub OAuth (github.com/settings/developers) ---
# Authorization callback URL: {SERVER_ORIGIN}/api/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- Email alerts (optional; without SMTP the server just logs the alert) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Expensio<no-reply@example.com>"
